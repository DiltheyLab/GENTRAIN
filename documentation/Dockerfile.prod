# --- Stage 1: Build ---
FROM node:22-alpine AS builder
WORKDIR /documentation
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

# --- Stage 2: Runtime (keep only output) ---
FROM alpine
WORKDIR /documentation
COPY --from=builder /documentation/build ./build
