on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  run_pull:
    name: run pull
    runs-on: ubuntu-latest

    steps:
      - name: connect and pull
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd ${{ secrets.GENTRAIN_DIR }}
            git checkout ${{ secrets.MAIN_BRANCH }}
            git pull
            echo ${{ secrets.GENTRAIN_PASSWORD }} | sudo -S docker compose stop
            echo ${{ secrets.GENTRAIN_PASSWORD }} | sudo -S docker compose up -d --no-deps --build
            exit
