services:
  backend:
    image: gentrain-backend
    restart: unless-stopped
    build:
      context: ./backend
      dockerfile: Dockerfile.dev
    ports:
      - "4000:4000"
    environment:
      - FLASK_ENV=development
      - FLASK_APP=main_site.py
